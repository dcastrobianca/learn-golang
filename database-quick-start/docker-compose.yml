services:
  db:
    container_name: recordings_db
    image: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: recordings
    volumes:
      - ./sql:/docker-entrypoint-initdb.d
    ports:
      - "5332:5432"
    restart: unless-stopped
